#!/bin/sh

for s in main extras; do
    tmux new-session -ds "$s" -c "$HOME" $SHELL
    tmux new-window -t "$s":2 -c "$HOME" $SHELL
    tmux select-window -t "$s":1
done

if [ -n "$TMUX" ]; then
    tmux switch-client -t main
else
    tmux attach -t main
fi

